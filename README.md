Python F1 25 car telemetry parser
currently as of now I was able to parse packet header, car damage, car telemetry and car status packets.
just call the respective display function of each packet in main() to print to terminal
